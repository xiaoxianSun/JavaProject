<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Sun.o2o.dao.AreaDao">
    <select id="queryArea" resultType="com.Sun.o2o.entity.Area">
        select tb_area.area_id,tb_area.area_name,tb_area.priority,tb_area.create_time,tb_area.last_edit_time
        from tb_area
        order by priority desc
    </select>
</mapper>